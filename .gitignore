node_modules/
.idea/
.vscode/

# Read-only clones of related repos kept here for reference only.
# (Their own git history lives upstream — don't track them.)
_SynAndKaya_clone/

# Workshop runtime state.
# Snapshots are committed (they're part of the showcase / agent output).
# Preprocessed reference images and metrics history are local-only noise.
.workshop/
.workshop/**
!.workshop/*.snapshots.json
!**/.workshop/*.snapshots.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Throwaway renders/diagnostics produced during agent runs
*_render.png
tmp_*.png
test_*.png
